/**
 * Config module — MongoDB data access.
 *
 * SCAFFOLD ONLY. The Config domain (form definitions) currently lives in the
 * separate `grants-ui-config-api` service. When that service is rewritten and
 * merged into `grants-ui-backend`, its MongoDB access will live here, targeting
 * a dedicated `grants-config` database (separate from the State module's
 * database) so a future split back into separate services stays straightforward.
 *
 * Until then this file is intentionally empty.
 */
