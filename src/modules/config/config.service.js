/**
 * Config module — business logic and the narrow internal interface exposed to
 * the State module.
 *
 * SCAFFOLD ONLY. The Config domain (form definitions) currently lives in the
 * separate `grants-ui-config-api` service. When that service is rewritten and
 * merged into `grants-ui-backend`, its business logic will live here. Any
 * cross-domain calls from the State module should go through this file (the
 * internal interface) rather than reaching into `config.repository.js`
 * directly, so a future split back into separate services stays straightforward.
 *
 * Until then this file is intentionally empty.
 */
